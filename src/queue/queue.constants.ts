export enum QueueName {
  WORKFLOW = 'workflow',
  KEYWORDS = 'keywords',
  CONTENT = 'content',
  IMAGE = 'image',
  SOCIAL = 'social',
  OUTREACH = 'outreach',
  ANALYTICS = 'analytics',
}

export const ALL_QUEUE_NAMES = Object.values(QueueName);

